# xynuthesis 使用说明文档

本文档根据当前仓库中的 `main.tex`、`xynusetup.tex`、`README.md` 及示例章节整理，适用于快速上手并完成论文排版。

## 1. 环境要求

- TeX 发行版：推荐最新 TeX Live / MacTeX / MiKTeX
- 编译引擎：建议使用 XeLaTeX（模板示例和 `latexmkrc` 均按 XeLaTeX 配置）
- 编辑器：TeXstudio / VS Code + LaTeX Workshop / Overleaf（本地模板迁移）

## 2. 项目结构与文件职责

- `main.tex`：论文主控文件，控制整体结构与章节顺序
- `xynusetup.tex`：统一配置入口（题名、作者、导师、字体、参考文献风格等）
- `xynuthesis.cls`：模板类文件（版式、封面、章节标题、页边距、页眉页脚等）
- `chapters/`：各章节正文（摘要、正文、致谢、科研成果等）
- `bib/xynu.bib`：参考文献数据库
- `figures/`：图片资源目录
- `latexmkrc`：`latexmk` 编译规则

## 3. 快速开始


0. 选择模板类型：
- 打开 `main.tex`，定位文件上方的 `\documentclass[degree=master,degree-type=professional]{xynuthesis}` 配置块。
- 由于理工科与文科的目录格式及章节标题要求不一样。请选择不同degree-type类型。
- 理工科请选择degree-type=professional
- 文科请选择degree-type=academic

1. 进入模板目录：


2. 详细修改个人信息（`xynusetup.tex`）：

- 打开 `xynusetup.tex`，定位文件最上方的 `\xynusetup{...}` 配置块。
- 只改你需要的字段值，保留每行末尾逗号`,`，不要删除外层大括号。
- 中文封面主要字段：
  - `title`：中文题目
  - `author`：中文姓名（学位申请人）
  - `speciality`：专业/专业学位领域
  - `supervisor`：校内导师姓名与职称
  - `practice-supervisor`：校外导师姓名与职称（没有可留空 `{}`）
  - `department`：所属院（系、所）
  - `date`：论文提交日期，格式 `YYYY-MM-DD`（不写则默认当天）
- 英文封面对应字段（与上面一一对应）：
  - `title*`、`author*`、`speciality*`、`supervisor*`、`practice-supervisor*`、`department*`
- 可选个人信息字段（默认被注释，按需去掉行首 `%`）：
  - `student-id`：学号
  - `classification-number`：分类号
  - `school-code`：学校代码（默认 `10477`）
  - `secret-level` / `secret-level*`：密级中英文

可直接参考下面的填写示例：

```tex
\xynusetup{
  title                = {基于 XXX 的 XXXX 研究},
  title*               = {Research on XXXX Based on XXX},
  author               = {张三},
  author*              = {San Zhang},
  speciality           = {控制工程},
  speciality*          = {Control Engineering},
  supervisor           = {李四~教授},
  supervisor*          = {Prof. Si Li},
  practice-supervisor  = {},
  practice-supervisor* = {},
  department           = {电子与信息工程学院},
  department*          = {School of Electronic and Information Engineering},
  date                 = {2026-03-01},
  student-id           = {2023123456},
}
```

填写注意事项：

- `~` 表示“不可断行空格”，如 `李四~教授` 可避免姓名与职称换行分开。
- 若你暂时不提交英文封面，可先填占位英文，定稿前再统一校对。
- 修改后执行 `latexmk -xelatex main.tex` 编译，重点检查中英文封面字段是否都已更新。

3. 按需调整 `main.tex` 的类选项（`\documentclass[...]`）：

- `degree`：学位层次（当前示例为 `master`）
- `degree-type`：`academic` / `professional` / `engineering`
- `language`：`chinese` / `english`
- `fontset`：`windows` / `mac` / `ubuntu` / `fandol`（可选）

4. 修改 `main.tex` 的章节组织（按需启用或注释）：

- 前置部分：`abstract`、目录、`notation`
- 正文部分：`chapter01`、`chapter02`、`chapter03`、`citations`
- 附录：`complementary`
- 后置部分：`acknowledgements`、`achievements`

5. 编译生成 PDF：

```bash
latexmk -xelatex main.tex
```

6. 清理临时文件：

```bash
latexmk -c
```

## 4. 论文结构说明（对应 `main.tex`）

模板默认结构为：

1. `\maketitle` 与 `\copyrightpage`
2. `\frontmatter`：摘要、目录、符号说明
3. `\mainmatter`：正文章节
4. 参考文献（默认 BibTeX）
5. `\appendix`：附录
6. `\backmatter`：致谢、科研成果

按学校要求可直接增删 `\include{chapters/...}` 条目。

## 5. 参考文献使用

### 5.1 默认方案：BibTeX + natbib（当前启用）

`xynusetup.tex` 当前默认：

- `\usepackage[sort]{natbib}`
- `\bibliographystyle{xynuthesis-numerical}`
- `main.tex` 使用 `\bibliography{bib/xynu.bib}`

可在正文中用 `\cite`、`\citet`、`\citep` 引用（见 `chapters/citations.tex`）。

### 5.2 切换著者-年份制

在 `xynusetup.tex` 中改为：

- `\usepackage{natbib}`
- `\bibliographystyle{xynuthesis-authoryear}`

### 5.3 切换 BibLaTeX（可选）

模板已给出注释示例，按需启用 `biblatex` 相关行，并在 `main.tex` 使用 `\printbibliography`。

## 6. 摘要、关键词与符号说明写法

- 摘要：在 `chapters/abstract.tex` 使用 `abstract` 与 `abstract*` 环境
- 关键词：通过 `\xynusetup{keywords=..., keywords*=...}` 设置
- 符号说明：在 `chapters/notation.tex` 使用 `notation` + `notationlist`

## 7. 图表与公式

- 图片默认搜索目录：`figures/`（由 `\graphicspath{{figures/}}` 设置）
- 常用图表宏包已在 `xynusetup.tex` 预置（`graphicx`、`subcaption`、`booktabs`、`longtable` 等）
- 算法环境已启用 `algorithm2e`

## 8. 常见个性化修改入口

优先在 `xynusetup.tex` 覆盖，不建议直接改 `xynuthesis.cls`：

- 页边距：可用 `\geometry{...}` 覆盖（参考 `页边距设置说明.md`）
- 章节标题：可用 `\ctexset{chapter/section/...}` 覆盖（参考 `章节标题格式设置说明.md`）
- 正文字号、行距、段落：可在 `xynusetup.tex` 重定义（参考 `正文格式设置说明.md`）
- 类文件结构与高级修改：参考 `xynuthesis.cls说明.md`

## 9. 编译问题与排查

- 首选命令：`latexmk -xelatex main.tex`
- 如果参考文献未更新：删除中间文件后重新编译
- 如果字体报错：检查系统是否安装对应中英文字体，或在 `xynusetup.tex` 切换 `fontset` / `cjk-font`

说明：当前仓库中 `Makefile` 的类文件名变量为 `xnuthesis`，与实际 `xynuthesis.cls` 不一致，直接执行 `make` 可能失败。建议优先使用 `latexmk` 命令。
