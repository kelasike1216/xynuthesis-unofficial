# 正文文字格式设置说明

## 主要设置位置

正文文字格式主要在 `xynuthesis.cls` 文件中设置，主要涉及以下几个方面：

### 1. 正文字号和行距

**位置**：`xynuthesis.cls` 第 **647-673 行**

```latex
\newcommand\xynu@set@font@size{%
  \renewcommand\normalsize{%
    \@setfontsize\normalsize{12bp}{20bp}%  % 字号12bp（小四），行距20bp
    ...
  }%
}
```

**默认设置**：
- 正文字号 12bp，行距 20bp

**修改方法**：
- 直接修改 `.cls` 文件中的数值（不推荐，更新模板时会丢失）
- 在 `xynusetup.tex` 中重新定义 `\normalsize`（推荐）

### 2. 行距倍数

**位置**：`xynuthesis.cls` 第 **675-676 行**

```latex
% 设置行距的倍数为 1。
\linespread{1}\selectfont
```

**说明**：行距倍数为 1，表示单倍行距。

**修改方法**：在 `xynusetup.tex` 中添加：
```latex
\linespread{1.5}\selectfont  % 改为1.5倍行距
```

### 3. 段落格式

**位置**：`xynuthesis.cls` 第 **2708-2720 行**

```latex
% 禁止扩大段间距
\raggedbottom

% 段间距 0 磅
\setlength{\parskip}{0bp}

% 首段缩进
\ctexset{autoindent=2}  % 首行缩进2个字符
```

**默认设置**：
- 段间距：0bp（段落之间无额外间距）
- 首行缩进：2个字符

**修改方法**：在 `xynusetup.tex` 中添加：
```latex
% 修改段间距
\setlength{\parskip}{6bp}  % 段落之间增加6磅间距

% 修改首行缩进
\ctexset{autoindent=1.5}  % 首行缩进1.5个字符
```

### 4. 字体设置

**位置**：`xynuthesis.cls` 第 **732-1101 行**

字体设置包括：
- **西文字体**：Times New Roman（Windows/Mac）或 TeX Gyre Termes（Linux）
- **中文字体**：宋体（SimSun/Songti SC/Noto Serif CJK SC/FandolSong）

**修改方法**：在 `xynusetup.tex` 中使用 `\xynusetup` 命令：
```latex
\xynusetup{
  font = times,      % 西文字体：times, termes, stix, xits, libertinus, newcm, lm, newtx
  cjk-font = windows, % 中文字体：windows, mac, noto, fandol
}
```

## 在 `xynusetup.tex` 中修改正文格式（推荐方法）

在 `xynusetup.tex` 文件的 `% 加载宏包` 部分之后，添加以下代码来修改正文格式：

```latex
% ========== 修改正文格式 ==========

% 1. 修改正文字号和行距（例如：改为14bp字号，24bp行距）
\renewcommand\normalsize{%
  \@setfontsize\normalsize{14bp}{24bp}%
  \abovedisplayskip 6bp \@plus3bp \@minus3bp%
  \abovedisplayshortskip 6bp \@plus3bp \@minus3bp%
  \belowdisplayshortskip \abovedisplayshortskip
  \belowdisplayskip \abovedisplayskip
  \let\@listi\@listI}%
\normalsize

% 2. 修改行距倍数（例如：改为1.2倍行距）
\linespread{1.2}\selectfont

% 3. 修改段间距（例如：段落之间增加6磅间距）
\setlength{\parskip}{6bp}

% 4. 修改首行缩进（例如：首行缩进1.5个字符）
\ctexset{autoindent=1.5}

% 5. 修改字体（可选）
\xynusetup{
  font = times,      % 西文字体
  cjk-font = windows, % 中文字体
}
```

## 常用格式参数对照表

| 格式项 | 默认值 | 修改位置 |
|--------|--------|----------|
| 正文字号 | 12bp | 第647-673行 |
| 正文行距 | 20bp | 第647-673行 |
| 行距倍数 | 1.0 | 第675-676行 |
| 段间距 | 0bp | 第2713行 |
| 首行缩进 | 2字符 | 第2716行 |

## 注意事项

1. **修改 `.cls` 文件**：不推荐，因为模板更新时会丢失修改
2. **在 `xynusetup.tex` 中修改**：推荐方法，修改会保留且不影响模板更新
3. **编译顺序**：修改字体相关设置后，需要重新编译文档
4. **格式要求**：修改前请确认是否符合学校论文格式要求

## 示例：完整修改示例

如果需要将正文改为：
- 字号：14bp
- 行距：24bp
- 行距倍数：1.2
- 段间距：6bp
- 首行缩进：1.5字符

在 `xynusetup.tex` 中添加：

```latex
% 修改正文格式
\makeatletter
\renewcommand\normalsize{%
  \@setfontsize\normalsize{14bp}{24bp}%
  \abovedisplayskip 6bp \@plus3bp \@minus3bp%
  \abovedisplayshortskip 6bp \@plus3bp \@minus3bp%
  \belowdisplayshortskip \abovedisplayshortskip
  \belowdisplayskip \abovedisplayskip
  \let\@listi\@listI}%
\normalsize
\makeatother

\linespread{1.2}\selectfont
\setlength{\parskip}{6bp}
\ctexset{autoindent=1.5}
```

