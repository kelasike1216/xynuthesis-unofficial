# 更新日志

本文件记录本仓库（基于 ustcthesis 二次改写的 xynuthesis）版本变更。
仅保留与本模板直接相关的维护记录。

## [Unreleased]

### Changed

- 新增中文封面“申请学位类别”配置项 `degree-category`：支持在 `xynusetup` 中自定义显示文本（如“教育硕士”）；未设置时仍按 `degree-type` 使用原默认值（`硕士学位`/`工程硕士`/`工程硕士学位`）。（修改日期：2026-02-09）

### Changed
- 修复中文封面标题字体应用范围：当标题字体为华文新魏（`STXinwei`）时，标题中的英文字母与数字现在也会使用同一字体，不再仅中文生效。
- 新增按学位类型切换章序号样式：`degree-type=academic` 时章标题与目录章号使用中文数字（第一章、第二章），其余类型保持阿拉伯数字；附录编号仍保持 A/B 形式。
- 新增按学位类型切换一级标题编号样式：`degree-type=academic` 时一级标题显示为“第一节、第二节…”，其余类型保持原有编号形式。
- 新增按学位类型切换二级标题编号样式：`degree-type=academic` 时二级标题显示为“一、二、三…”，其余类型保持原有编号形式。
- 新增按学位类型切换三级至五级标题编号样式：`degree-type=academic` 时三级标题显示为“（一）”、四级标题显示为“1.”、五级标题显示为“（1）”；并将该类型编号深度扩展到五级。
- 修复目录层级显示：统一设置 `tocdepth=5`（适用于所有 `degree-type`），并补齐 `subsubsection`/`paragraph`/`subparagraph` 的目录样式定义，使三级及以下标题可在目录中显示。
- 调整中文封面导师字段：`degree-type=academic` 时“校内导师姓名、职称”改为“导师姓名、职称”，并隐藏“校外导师姓名、职称”；其他类型保持原样。（修改日期：2026-02-08）

## [1.0.0] - 2026-02-06

### Added

- 基于 ustcthesis 的信阳师范大学学位论文模板初始版本。
- 目录结构与示例文档（main.tex、chapters/、bib/、figures/）。
- 使用说明与格式说明文档（章节标题、正文格式、页边距与类说明）。

### Changed

- 统一模板入口配置为 xynusetup.tex。
