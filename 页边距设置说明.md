# 页边距设置说明

## 页边距要求

根据要求，A4纸张的页边距设置如下：

- **左页边距**：2.8 cm
- **右页边距**：3.0 cm
- **上页边距**：3.5 cm
- **下页边距**：3.5 cm
- **页眉距页顶**：2.7 cm
- **页脚距页底**：2.7 cm
- **装订线**：0 cm

## 修改方法

### 方法一：在 `xynusetup.tex` 中修改（推荐）

在 `xynusetup.tex` 文件的 `% 加载宏包` 部分之后，添加以下代码：

```latex
% 修改页边距（根据要求：左2.8cm，右3cm，上3.5cm，下3.5cm，页眉2.7cm，页脚2.7cm）
\geometry{
  left      = 2.8cm,   % 左页边距
  right     = 3.0cm,   % 右页边距
  top       = 3.5cm,   % 上页边距
  bottom    = 3.5cm,   % 下页边距
  headheight = 0.49cm, % 页眉高度（保持默认）
  headsep   = 0.31cm,  % 页眉与正文间距（3.5-2.7-0.49=0.31）
  footskip  = 0.79cm,  % 页脚基线到正文底部距离
}
```

### 方法二：直接修改 `xynuthesis.cls` 文件（不推荐）

如果需要永久修改默认值，可以编辑 `xynuthesis.cls` 文件的第 1554-1561 行：

```latex
\geometry{
  paper      = a4paper,
  left       = 2.8cm,   % 修改左页边距
  right      = 3.0cm,   % 修改右页边距
  top        = 3.5cm,   % 修改上页边距
  bottom     = 3.5cm,   % 修改下页边距
  headheight = 0.49cm,
  headsep    = 0.31cm,  % 调整页眉位置
  footskip   = 0.79cm,
}
```

**注意**：直接修改 `.cls` 文件会在模板更新时丢失修改，因此推荐使用方法一。

## 参数说明

- `left` / `right`：左右页边距
- `top` / `bottom`：上下页边距
- `headheight`：页眉高度（通常为 0.49cm）
- `headsep`：页眉与正文之间的间距
  - 计算公式：`headsep = top - 页眉距页顶 - headheight`
  - 例如：`headsep = 3.5 - 2.7 - 0.49 = 0.31cm`
- `footskip`：页脚基线到正文底部的距离

## 验证设置

修改后，重新编译 LaTeX 文档，检查：
1. 页边距是否符合要求
2. 页眉位置是否正确（距页顶 2.7cm）
3. 页脚位置是否正确（距页底 2.7cm）

## 默认设置对比

| 参数 | 默认值 | 要求值 |
|------|--------|--------|
| 左页边距 | 3.17cm | 2.8cm |
| 右页边距 | 3.17cm | 3.0cm |
| 上页边距 | 2.54cm | 3.5cm |
| 下页边距 | 2.54cm | 3.5cm |
| 页眉距页顶 | 2.0cm | 2.7cm |
| 页脚距页底 | 1.5cm | 2.7cm |

