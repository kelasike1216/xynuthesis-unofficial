# 章节标题格式设置说明

## 主要设置位置

章节标题格式主要在 `xynuthesis.cls` 文件中使用 `\ctexset` 命令设置，主要分为两部分：

### 1. 硕士格式（默认）

**位置**：`xynuthesis.cls` 第 **2344-2391 行**

---

## 硕士格式详细说明

### 章标题（chapter）

**位置**：第 2344-2357 行

```latex
\ctexset{
  chapter = {
    format      = \centering\heiti\fontsize{16bp}{20.8bp}\selectfont,
    nameformat  = {},
    titleformat = {},
    number      = \thechapter,
    aftername   = \quad,
    beforeskip  = 7bp,    % 段前间距
    afterskip   = 19bp,   % 段后间距
  },
}
```

**默认设置**：
- **字体**：黑体（`\heiti`）
- **字号**：16bp（三号字）
- **行距**：20.8bp
- **对齐**：居中（`\centering`）
- **段前间距**：7bp
- **段后间距**：19bp
- **编号与标题间距**：1个汉字符（`\quad`）

### 一级节标题（section）

**位置**：第 2364-2370 行

```latex
\ctexset{
  section = {
    format     = \heiti\fontsize{14bp}{20bp}\selectfont,
    aftername  = \quad,
    beforeskip = 24bp,   % 段前间距
    afterskip  = 6bp,    % 段后间距
  },
}
```

**默认设置**：
- **字体**：黑体（`\heiti`）
- **字号**：14bp（四号字）
- **行距**：20bp
- **对齐**：居左
- **段前间距**：24bp
- **段后间距**：6bp

### 二级节标题（subsection）

**位置**：第 2374-2379 行

```latex
subsection = {
  format     = \heiti\fontsize{13bp}{20bp}\selectfont,
  aftername  = \quad,
  beforeskip = 12bp,    % 段前间距
  afterskip  = 6bp,     % 段后间距
},
```

**默认设置**：
- **字体**：黑体（`\heiti`）
- **字号**：13bp
- **行距**：20bp
- **对齐**：居左
- **段前间距**：12bp
- **段后间距**：6bp

### 三级节标题（subsubsection）

**位置**：第 2383-2388 行

```latex
subsubsection = {
  format     = \heiti\fontsize{12bp}{20bp}\selectfont,
  aftername  = \quad,
  beforeskip = 12bp,    % 段前间距
  afterskip  = 6bp,     % 段后间距
},
```

**默认设置**：
- **字体**：黑体（`\heiti`）
- **字号**：12bp（小四号字）
- **行距**：20bp
- **对齐**：居左
- **段前间距**：12bp
- **段后间距**：6bp

---

## 修改方法

### 方法一：在 `xynusetup.tex` 中修改（推荐）

在 `xynusetup.tex` 文件的 `% 加载宏包` 部分之后，添加以下代码：

#### 修改研究生格式示例

```latex
% ========== 修改章节标题格式 ==========

% 修改章标题格式
\ctexset{
  chapter = {
    format      = \centering\heiti\fontsize{18bp}{22bp}\selectfont,  % 改为18bp字号
    beforeskip  = 10bp,   % 修改段前间距
    afterskip   = 20bp,   % 修改段后间距
    aftername   = \quad,  % 编号与标题间距
  },
  % 修改一级节标题格式
  section = {
    format     = \heiti\fontsize{16bp}{22bp}\selectfont,  % 改为16bp字号
    beforeskip = 20bp,    % 修改段前间距
    afterskip  = 8bp,     % 修改段后间距
  },
  % 修改二级节标题格式
  subsection = {
    format     = \heiti\fontsize{14bp}{20bp}\selectfont,
    beforeskip = 10bp,
    afterskip  = 6bp,
  },
  % 修改三级节标题格式
  subsubsection = {
    format     = \heiti\fontsize{13bp}{20bp}\selectfont,
    beforeskip = 10bp,
    afterskip  = 6bp,
  },
}
```

### 方法二：直接修改 `xynuthesis.cls` 文件（不推荐）

直接编辑 `.cls` 文件中对应的 `\ctexset` 命令，但模板更新时会丢失修改。

---

## 常用参数说明

### `format` - 格式设置
控制标题的字体、字号、行距和对齐方式。

**常用格式命令**：
- `\centering`：居中
- `\raggedright`：居左
- `\heiti`：黑体
- `\songti`：宋体
- `\sffamily`：无衬线体
- `\fontsize{字号}{行距}\selectfont`：设置字号和行距

**示例**：
```latex
format = \centering\heiti\fontsize{16bp}{20bp}\selectfont
```

### `beforeskip` - 段前间距
标题前的垂直间距（单位：bp）。

### `afterskip` - 段后间距
标题后的垂直间距（单位：bp）。

### `aftername` - 编号与标题间距
章节编号与标题文字之间的间距。

**常用值**：
- `\quad`：1个汉字符宽度
- `\qquad`：2个汉字符宽度
- `\hspace{1em}`：1em宽度
- `.`：点号（用于某些格式）

### `number` - 编号格式
控制章节编号的显示方式。

**常用值**：
- `\thechapter`：阿拉伯数字（第1章）
- `\chinese{chapter}`：中文数字（第一章）
- `\Roman{chapter}`：大写罗马数字
- `\roman{chapter}`：小写罗马数字

### `nameformat` - 编号格式
控制章节编号的字体格式。

### `titleformat` - 标题格式
控制标题文字的格式。

---

## 格式参数对照表

### 硕士格式

| 标题级别 | 字体 | 字号 | 行距 | 对齐 | 段前 | 段后 |
|---------|------|------|------|------|------|------|
| 章（chapter） | 黑体 | 16bp | 20.8bp | 居中 | 7bp | 19bp |
| 一级节（section） | 黑体 | 14bp | 20bp | 居左 | 24bp | 6bp |
| 二级节（subsection） | 黑体 | 13bp | 20bp | 居左 | 12bp | 6bp |
| 三级节（subsubsection） | 黑体 | 12bp | 20bp | 居左 | 12bp | 6bp |

---

## 完整修改示例

如果需要将研究生格式的章标题改为：
- 字号：18bp
- 行距：22bp
- 段前间距：10bp
- 段后间距：20bp

在 `xynusetup.tex` 中添加：

```latex
% 修改章标题格式
\ctexset{
  chapter = {
    format      = \centering\heiti\fontsize{18bp}{22bp}\selectfont,
    beforeskip  = 10bp,
    afterskip   = 20bp,
  },
}
```

---

## 注意事项

1. **修改位置**：推荐在 `xynusetup.tex` 中修改，而不是直接修改 `.cls` 文件
2. **编译顺序**：修改后需要重新编译文档
3. **格式要求**：修改前请确认是否符合学校论文格式要求
4. **编号格式**：可以通过 `number` 参数修改编号显示方式（如中文数字、罗马数字等）

